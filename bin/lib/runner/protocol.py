"""Canonical Frollo event protocol constants.

The full contract is still being derived from the Claude and Codex adapters.
Keeping the schema name shared avoids provider-local event dialects while that
contract stabilizes.
"""

SCHEMA = "frollo.event.v0"
